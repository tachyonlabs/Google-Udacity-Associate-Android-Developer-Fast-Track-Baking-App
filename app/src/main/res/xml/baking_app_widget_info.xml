<?xml version="1.0" encoding="utf-8"?>
<!--updatePeriodMillis is 0 because the app takes care of updates-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
                    android:minWidth="180dp"
                    android:minHeight="250dp"
                    android:updatePeriodMillis="0"
                    android:previewImage="@drawable/baking_app_widget_preview"
                    android:initialLayout="@layout/baking_app_widget"
                    android:resizeMode="horizontal|vertical"
                    android:widgetCategory="home_screen"
                    android:initialKeyguardLayout="@layout/baking_app_widget">
</appwidget-provider>